# Para las funciones trigonometricas hay que transformar a radianes, porque esos son los parametros

# Entonces se tiene que multiplicar por pi y dividir entre 180

cos(60) #ojo esta en radianes
cos(60*pi/180)
cospi(60/180) #esta es la misma pero sin la necesidad de escribir el pi. 

asin(0.80) * 180 / pi

